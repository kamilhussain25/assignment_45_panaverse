"use strict";
//task34
let favouritepizzas = ["chicken pizza", "beef pizza", "vagetable pizza"];
for (let index = 0; index < favouritepizzas.length; index++) {
    const element = favouritepizzas[index];
    console.log(`i like  ${element} a lot`);
}
for (let index = 0; index < favouritepizzas.length; index++) {
    const element = favouritepizzas[index];
    console.log(element);
}
